<div class='container text-center'>
  <h2>My Current Schedule</h2>

  <% @courses.each do |course| %>
    <%= course.course_name %>
    <%= link_to "View Assignments", student_course_path(@student, course.id) %><br>
    <% end %>
</div>
