<%= render "partials/navbar" %>

<div class='container text-center'>
  <h4><%= @category.title %> Assignments</h4><br>

<%= button_to "Edit Category", edit_student_category_path, :class => "inbox-sf-add-btn tip", :method => :get %><br>
<%= button_to "Delete Category", student_category_path(@student), method: :delete, data: { confirm: "Remove assignment category?" } %>
</div>
