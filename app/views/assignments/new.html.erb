<nav class="navbar navbar-dark bg-primary">
  <a class="navbar-brand" href="<%= student_path(@student) %>">Student Planner</a>
  <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarsExample01" aria-controls="navbarsExample01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="navbar-collapse collapse" id="navbarsExample01" style="">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to 'Home',  student_path(@student), :class => "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to 'Courses',  student_courses_path(@student), :class => "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to 'Assignments',  student_assignments_path(@student), :class => "nav-link" %>
      </li>
    </ul>
  </div>
</nav>

<div class='container text-center'>
  <h2>New Assignment Form</h2>
  <h3>To add an assignment to your planner, please select or <a href="<%= new_student_course_path %>">create</a> a course first</h3>

  <%= form_for(@assignment, url: student_assignments_path) do |f| %>
    <%= f.hidden_field :student_id %>
    <%= f.label "Select Course:" %>
    <%= f.collection_select :course_id, @courses, :id, :course_name, prompt: true %>
    <%= f.label "Title" %>
    <%= f.text_field :title %><br><br>
    <%= f.label "Due Date:" %>
    <%= f.date_select :due_date %><br><br>
    <%= f.submit %>
    <% end %>
</div>
